---
applyTo: "python-app/tests/**/*.py"
---

- Keep tests focused on one behavior at a time.
- Prefer clear setup and explicit assertions over clever test helpers.
- When adding or changing tests, use the existing `pytest` workflow.
- Test names should describe the behavior being checked.